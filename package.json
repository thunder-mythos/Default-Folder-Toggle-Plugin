{
  "name": "default-folder-toggle",
  "version": "1.0.0",
  "description": "Obsidian plugin: auto-manages folder expand/collapse from YAML frontmatter or settings overrides",
  "main": "main.js",
  "scripts": {
    "dev": "node esbuild.config.mjs",
    "build": "tsc -noEmit -skipLibCheck && node esbuild.config.mjs production",
    "version": "node version-bump.mjs && git add manifest.json versions.json"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "builtin-modules": "^3.3.0",
    "esbuild": "0.21.5",
    "obsidian": "latest",
    "tslib": "2.6.0",
    "typescript": "5.4.5"
  }
}
